<Window x:Class="PersonManager.PersonModalDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Person" Height="250" Width="300" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="First Name:"/>
        <TextBox x:Name="TxtFirstName" Grid.Row="0" Grid.Column="1" Margin="0,2,0,8"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Last Name:"/>
        <TextBox x:Name="TxtLastName" Grid.Row="1" Grid.Column="1" Margin="0,2,0,8"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Birth Year:"/>
        <TextBox x:Name="TxtBirthYear" Grid.Row="2" Grid.Column="1" Margin="0,2,0,8"/>

        <Label Grid.Row="3" Grid.Column="0" Content="City:"/>
        <TextBox x:Name="TxtCity" Grid.Row="3" Grid.Column="1" Margin="0,2,0,8"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="ActionButton" Content="Add" Width="70" Margin="0,0,10,0" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="70" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>